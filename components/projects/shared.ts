/**
 * One gutter for the whole project page. The stage runs full bleed, so nothing on
 * the page is centred against a max width — the type lines up with the screen's
 * edges instead, and long-form copy caps itself where it needs to.
 */
export const GUTTER = "px-5 sm:px-16 laptop:px-10"

/** The small uppercase mono label used for every section and field name. */
export const LABEL = "label-s uppercase tracking-[0.2em] text-muted-foreground/60"
